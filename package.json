{
  "name": "calcify-suite",
  "version": "1.0.0",
  "description": "Calcify_Suite - Sales Agent Performance & Salary Calculation Platform (GitHub Pages)",
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev",
    "preview": "cd client && npm run preview"
  }
}